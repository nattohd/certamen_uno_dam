import 'package:flutter/material.dart';

var boxDecorationLogin = BoxDecoration(
  borderRadius: BorderRadius.circular(12),
  color: Color.fromARGB(255, 211, 163, 250),
  boxShadow: [
    BoxShadow(
      color: Colors.black.withOpacity(0.8),
      spreadRadius: 5,
      blurRadius: 7,
      offset: Offset(0, 3),
    ),
  ],
);
